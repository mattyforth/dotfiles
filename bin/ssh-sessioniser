#!/usr/bin/env bash

local server
server=$(grep -E '^Host ' ~/.ssh/config | awk '{print $2}' | fzf)
if [[ -n $server ]]; then
    # TODO: Call tmux sessionizer, alter the script so we can pass a command in for 'ssh $server' instead of nvim.

    ssh $server
fi
